name: CI Pipeline PR
on:
  pull_request:
    branches:
      - master
      - beta
      - alpha
      - nightly
jobs:
  continuous-integration:
    uses: ./.github/workflows/continuous-integration.yml
