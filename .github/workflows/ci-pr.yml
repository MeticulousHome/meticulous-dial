name: CI Pipeline PR
on:
  pull_request:
    branches:
      - stable
      - beta
      - alpha
      - nightly
jobs:
  continuous-integration:
    uses: ./.github/workflows/continuous-integration.yml
